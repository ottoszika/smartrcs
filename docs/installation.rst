.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install smartrcs

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv smartrcs
    $ pip install smartrcs
